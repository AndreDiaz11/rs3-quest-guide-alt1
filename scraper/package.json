{
  "name": "rs3-quest-guide-scraper",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Offline scraper/translator that generates the data/ dataset consumed by app/. Developer-only, never shipped.",
  "scripts": {
    "run": "node --env-file-if-exists=.env src/run.js"
  },
  "dependencies": {
    "cheerio": "^1.0.0"
  }
}
